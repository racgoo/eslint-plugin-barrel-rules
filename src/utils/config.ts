/**
 * Config class for finding the nearest tsconfig.json file
 * for tsconfig-paths(alise, ex: @domains, @features etc..)
 */
class Config {
  private constructor() {}
  public static findNearestTsconfig(startDir: string): string | null {
    return null;
  }
}

export { Config };
